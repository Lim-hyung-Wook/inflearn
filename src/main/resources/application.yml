server:
  port: 8088

spring:
  messages:
    basename: messages
  security:
    user:
      name: username
      password: passwOrd
  datasource:
    url: jdbc:h2:mem:testdb
    jpa:
      show-sql:true
    h2:
      console:
        enabled:true



management:
  endpoints:
    web:
      exposure:
        include: "*"

